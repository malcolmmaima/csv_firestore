{
  "name": "csv_to_firestore",
  "version": "1.0.0",
  "description": "upload csv data to firebase firestore database",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase-admin": "^10.0.2",
    "prompt-sync": "^4.2.0"
  }
}
